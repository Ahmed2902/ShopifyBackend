/// <reference types="node" />

// Compatibility entrypoint. Keep the old command working, but run the isolated
// sandbox harness that does not depend on merchant OAuth/account discovery.
import './meta-sandbox-intelligence-direct.js';
